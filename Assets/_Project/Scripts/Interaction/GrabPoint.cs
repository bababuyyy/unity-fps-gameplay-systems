using UnityEngine;

/// <summary>
/// Acts as an anchor point for grabbed objects.
/// Attach this to an empty child GameObject of the CameraRoot.
/// </summary>
public class GrabPoint : MonoBehaviour
{
    // Empty class serving as a strict transform marker.
}
